# Future TODO

## Nice to do later

- Consider moving conversation detail URLs from `/chat?conversationId=...` to route-based paths like `/chat/[conversationId]` for cleaner long-term module structure.
